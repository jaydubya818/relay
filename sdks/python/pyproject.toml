[project]
name = "relay-v2"
version = "0.1.0"
requires-python = ">=3.10"
description = "Reference client for the Relay V2 authoritative control plane"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
