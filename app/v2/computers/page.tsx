import { requireUser } from "@/lib/auth"; import { getV2Dashboard } from "@/lib/v2/dashboard"; import { ConfirmOperatorAction } from "@/components/v2/operator-actions"; import { V2Empty, V2Header, V2Notice, V2Status, V2Time } from "@/components/v2/ui";
export default async function ComputersPage() { const user = await requireUser(); const data = await getV2Dashboard(user.accountId); return <><V2Header kicker="Live execution" title="Computers" description="Observe who controls input, pause safely, and take exclusive human control with monotonic fencing." /><V2Notice tone="warning"><strong>Protected credential entry suppresses observation before Relay.</strong> No frame, typed value, or credential content is retained while the shield is active.</V2Notice><div className="v2-card-grid v2-section-gap">{data.computers.map((computer) => <article className="v2-panel v2-computer" key={computer.id}><div className="v2-computer-screen"><div className="v2-screen-bar"><i /><i /><i /><span>LIVE / {computer.controller}</span></div><div><strong>{computer.credentialEntryMode ? "Protected input active" : "Observation available through viewer grant"}</strong><small>{computer.credentialEntryMode ? "Frames intentionally suppressed" : "Short-lived, account-scoped access only"}</small></div></div><div className="v2-computer-meta"><div><span className="mono">{computer.id}</span><V2Status value={computer.controller} /></div><dl><div><dt>Task</dt><dd className="mono">{computer.taskId}</dd></div><div><dt>Fence</dt><dd>{computer.fenceToken}</dd></div><div><dt>Active Agent input</dt><dd>{computer.activeInputCount}</dd></div><div><dt>Expires</dt><dd><V2Time value={computer.expiresAt} /></dd></div></dl><div className="v2-action-row">{computer.controller !== "PAUSED" && <ConfirmOperatorAction path="/api/v2/operator/computers" body={{ controlSessionId: computer.id, fenceToken: computer.fenceToken, action: "pause" }} label="Pause" confirmLabel="Pause all input for this session?" />}{computer.controller !== "HUMAN" && <ConfirmOperatorAction path="/api/v2/operator/computers" body={{ controlSessionId: computer.id, fenceToken: computer.fenceToken, action: "take-control" }} label="Take control" confirmLabel="Fence the Agent and take exclusive input?" />}</div></div></article>)}</div>{!data.computers.length && <V2Empty title="No live computers" detail="Provider-neutral visual sessions will appear once a task receives bounded execution authority." />}</>; }
