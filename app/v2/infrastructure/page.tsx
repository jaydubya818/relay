import { requireUser } from "@/lib/auth"; import { getV2Dashboard } from "@/lib/v2/dashboard"; import { ConfirmOperatorAction } from "@/components/v2/operator-actions"; import { V2Empty, V2Header, V2Status, V2Time } from "@/components/v2/ui";
export default async function InfrastructurePage() { const user = await requireUser(); const data = await getV2Dashboard(user.accountId); return <><V2Header kicker="Provider-neutral execution" title="Runners & providers" description="Relay owns authority and evidence; managed and customer-hosted execution receive only short-lived bounded work." /><div className="v2-grid v2-grid-even"><section className="v2-panel"><div className="v2-panel-head"><div><span>CUSTOMER HOSTED</span><h2>Runners</h2></div></div>{data.runners.length ? data.runners.map((runner) => <div className="v2-runner" key={runner.id}><div><strong>{runner.name}</strong><small className="mono">{runner.softwareDigest.slice(0, 24)}…</small></div><V2Status value={runner.status} /><dl><div><dt>Assurance</dt><dd>{runner.assurance}</dd></div><div><dt>Last seen</dt><dd><V2Time value={runner.lastSeenAt} /></dd></div><div><dt>Certificate</dt><dd><V2Time value={runner.certificateExpiresAt} /></dd></div></dl>{runner.status === "ACTIVE" && <ConfirmOperatorAction path="/api/v2/operator/runners" body={{ runnerId: runner.id, reason: "Revoked from Relay V2 operator dashboard" }} label="Revoke runner" confirmLabel="Revoke this runner, assignments, workloads, and leases?" danger />}</div>) : <V2Empty title="No customer runners" detail="Outbound-only runners enroll with proof of possession and expiring certificates." />}</section><section className="v2-panel"><div className="v2-panel-head"><div><span>PLACEMENT</span><h2>Execution decisions</h2></div></div>{data.placements.length ? data.placements.map((placement) => <div className="v2-list-row" key={placement.id}><div><strong className="mono">{placement.taskId}</strong><small className="mono">{placement.providerDefinitionId}</small></div><V2Status value={placement.status} /><V2Time value={placement.updatedAt} /></div>) : <V2Empty title="No placements" detail="Hard security constraints are evaluated before speed, capacity, or price." />}</section></div></>; }
